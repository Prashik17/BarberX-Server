
import { Request, Response } from "express";

export class OwnerController {
  // Owner-specific controller methods can be added here as needed
  // Authentication and registration are handled by AuthController
}

export default new OwnerController();